
import React from "react";
import ReactDOM from "react-dom/client";
import RfpGenerator from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RfpGenerator />
  </React.StrictMode>
);
